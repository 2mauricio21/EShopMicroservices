version: '3.4'

services:
  catalogdb:
    image: postgres:16

  catalog.api:
    image: ${DOCKER_REGISTRY-}catalogapi
    build:
      context: .
      dockerfile: Service/Catalog/Catalog.API/Dockerfile

volumes:
  postgres_catalog:
